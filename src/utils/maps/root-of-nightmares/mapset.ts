import type { MapSet } from "@/utils/maps/maps";
import planetsMap from "./planets";

const mapSet: MapSet = {
    id: 'root-of-nightmares',
    name: 'Root of Nightmares',
    maps: [planetsMap]
}

export default mapSet;